# railway.toml

[project]
name = "library"

[start]
# Backend (Express)
[start.backend]
command = "cd backend && npm run start"
service = "backend"
env = "node"

# Frontend (React)
[start.frontend]
command = "cd frontend && npm install && npm run start"
service = "frontend"
env = "node"
